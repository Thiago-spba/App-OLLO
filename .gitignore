# ===================================================================
# ARQUIVO .GITIGNORE OTIMIZADO PARA PROJETO REACT/VITE + FIREBASE
# ===================================================================

# 1. SEGURANÇA: Arquivos de Senhas e Chaves de API
# Ignora todos os arquivos .env, .env.local, .env.production, etc.
.env*
# Exceção: Permite que um arquivo de exemplo seja versionado (boa prática).
!/.env.example

# Outros arquivos de segredos comuns
secrets.*
credentials.*
private.*

# 2. DEPENDÊNCIAS E BUILDS
# Pasta de pacotes do Node (recriada com 'npm install')
/node_modules
# Pasta de build do frontend Vite (recriada com 'npm run build')
/dist
# Pasta de build das Cloud Functions (recriada com 'tsc')
/functions/lib

# 3. FIREBASE: Arquivos de cache e configuração local
.firebase
.firebaserc.local
hosting.*.log

# 4. LOGS E CACHES DE FERRAMENTAS
npm-debug.log*
yarn-debug.log*
*.log
/.turbo
/.cache
/.parcel-cache
/coverage

# 5. SISTEMA OPERACIONAL E EDITORES
.DS_Store
.vscode/
Thumbs.db
*.swp

# 6. ARQUIVOS TEMPORÁRIOS DO PROJETO (Exemplos)
# src/cors.json
# src/lista-arquivos.txt