// ARQUIVO CORRIGIDO E DEFINITIVO: firestore.rules

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // --- FUNÇÕES HELPER ---
    // ARQUITETURA: Funções reutilizáveis para manter as regras limpas (DRY principle).

    function isOwner(userId) {
      return request.auth != null && request.auth.uid == userId;
    }

    function isAdmin() {
      return request.auth != null && exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }

    // MUDANÇA: Nova função de validação para o campo 'privacy'.
    // ARQUITETURA: Centraliza a lógica de quais valores de privacidade são permitidos.
    // Se no futuro adicionarmos 'followers-only', só precisamos mudar aqui.
    function isValidPrivacy(privacy) {
      return privacy in ['public', 'private'];
    }

    // Regra especial para ambiente de desenvolvimento com emuladores
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

    // --- REGRAS POR COLEÇÃO ---

    // 👤 Coleção 'users' (DADOS PRIVADOS E SENSÍVEIS)
    match /users/{userId} {
      allow read, write: if isOwner(userId) || isAdmin();
    }

    // 📢 Coleção 'users_public' (DADOS PÚBLICOS DE PERFIL)
    match /users_public/{userId} {
      allow read: if true;
      allow write: if isOwner(userId) || isAdmin();
    }

    // 🖼️ Subcoleção 'media' de um usuário
    match /users/{userId}/media/{mediaId} {
      // Leitura continua a mesma: permitida se for público, se for o dono ou admin.
      allow read: if resource.data.privacy == 'public' || isOwner(userId) || isAdmin();
      
      // Criar e deletar continua com a mesma permissão.
      allow create, delete: if isOwner(userId) || isAdmin();

      // CORREÇÃO: 'update' foi separado para ter sua própria lógica de validação.
      // ARQUITETURA: Agora verificamos não apenas QUEM pode atualizar, mas O QUE pode ser atualizado.
      allow update: if (isOwner(userId) || isAdmin())
                    && isValidPrivacy(request.resource.data.privacy) // O novo valor de 'privacy' deve ser válido.
                    && request.resource.data.ownerId == resource.data.ownerId // O 'ownerId' não pode ser alterado.
                    && request.resource.data.createdAt == resource.data.createdAt; // A data de criação é imutável.
    }
    
    // ✍️ Coleção 'posts'
    match /posts/{postId} {
      allow read: if true;
      allow create: if (request.auth != null && request.resource.data.authorId == request.auth.uid) || isAdmin();
      allow update, delete: if (request.auth != null && resource.data.authorId == request.auth.uid) || isAdmin();
    }

    // Coleção 'marketplace' (para anúncios)
    match /marketplace/{itemId} {
      allow read: if true;
      allow create: if request.auth != null && request.resource.data.sellerId == request.auth.uid;
      allow update, delete: if request.auth != null && resource.data.sellerId == request.auth.uid;
    }
  }
}